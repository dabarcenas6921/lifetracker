import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#2b2d42",
      light: "skyblue",
    },
    secondary: {
      main: "#edf2f4",
    },
    otherColor: {
      main: "#d90429",
    },
  },
});
