import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#fffff",
      light: "skyblue",
    },
    secondary: {
      main: "#fca311",
    },
    otherColor: {
      main: "#000000",
    },
  },
});
